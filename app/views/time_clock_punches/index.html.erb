<% breadcrumb :time_clock_punches %>

<% filters = [
        { attr: :start_time_gteq, label: "Starting After", opts: { input_html: { type: "datetime-local" } } },
        { attr: :end_time_lteq, label: "Ending Before", opts: { input_html: { type: "datetime-local" } } },
        { attr: :time_clock_period_id_eq, label: "Period", as: :select, collection: policy_scope(TimeClockPeriod) }
      ] %>

<%= render layout: "shared/filter_bar", locals: {
      q: @q,
      title: "Time Clock",
      collection: @time_clock_punches,
      primary: { attr: :person_display_name_cont, placeholder: "Search by person…" },
      filters: filters,
      sorts: { start_time: "Start Time", end_time: "End Time", updated_at: "Last Updated" },
      new_path: (new_time_clock_punch_path if policy(TimeClockPunch).new?),
      new_label: "Punch",
      actions: capture do %>
        <%= link_to "Periods", time_clock_periods_path, class: "btn btn-info" %>
        <%= link_to "Flag Review", flagged_time_clock_punches_path, class: "btn btn-warning" if policy(TimeClockPunch).flagged? %>
        <%= link_to "Kiosk", time_kiosk_path, class: "btn btn-warning", data: { turbo: false } if manager? %>
      <% end
    } do |f| %>
<% end %>

<%= turbo_frame_tag "results", data: { turbo_action: "advance" } do %>
  <%= render "shared/results_header", q: @q, filters: filters, collection: @time_clock_punches, noun: "punch" %>

  <% if @time_clock_punches.any? %>

    <div class="row">
      <div class="col">
        <%= render "time_clock_punches" %>
      </div>
    </div>

    <%= paginate @time_clock_punches %>
  <% else %>
    <%= render "shared/no_results", noun: "punch", filtered: any_filters_applied?(@q) %>
  <% end %>
<% end %>
